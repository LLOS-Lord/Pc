--[[
    SIMPLE SPY V13 - BẢN TÁI LẬP MÃ NGUỒN CHUẨN LẬP TRÌNH (PRODUCTION-READY CODE)
    Chức năng: Sinh mã nguồn chính xác cấu trúc tham số để copy dùng ngay cho các script bên thứ ba.
--]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

if PlayerGui:FindFirstChild("SimpleSpy_ProductionViewer") then
    PlayerGui:FindFirstChild("SimpleSpy_ProductionViewer"):Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SimpleSpy_ProductionViewer"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = PlayerGui

-- KHUNG BẢNG ĐIỀU KHIỂN CHÍNH
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 320, 0, 360)
MainFrame.Position = UDim2.new(0.25, -160, 0.3, -180)
MainFrame.BackgroundColor3 = Color3.fromRGB(24, 25, 28)
MainFrame.BorderSizePixel = 1
MainFrame.BorderColor3 = Color3.fromRGB(255, 170, 0)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 35)
Title.BackgroundColor3 = Color3.fromRGB(32, 33, 38)
Title.Text = "  SIMPLE SPY - LIVE LOGS"
Title.TextColor3 = Color3.fromRGB(255, 170, 0)
Title.Font = Enum.Font.Arial
Title.TextSize = 13
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

-- KHUNG HIỂN THỊ CODE ĐỂ LẬP TRÌNH
local CodeFrame = Instance.new("Frame")
CodeFrame.Name = "CodeFrame"
CodeFrame.Size = UDim2.new(0, 440, 0, 360)
CodeFrame.Position = UDim2.new(0.25, 165, 0.3, -180)
CodeFrame.BackgroundColor3 = Color3.fromRGB(16, 17, 20)
CodeFrame.BorderSizePixel = 1
CodeFrame.BorderColor3 = Color3.fromRGB(0, 255, 150)
CodeFrame.Parent = MainFrame

local CodeTitle = Instance.new("TextLabel")
CodeTitle.Size = UDim2.new(1, 0, 0, 35)
CodeTitle.BackgroundColor3 = Color3.fromRGB(24, 25, 30)
CodeTitle.Text = "  MÃ NGUỒN THỰC THI (READY-TO-USE CODE)"
CodeTitle.TextColor3 = Color3.fromRGB(0, 255, 150)
CodeTitle.Font = Enum.Font.Arial
CodeTitle.TextSize = 13
CodeTitle.TextXAlignment = Enum.TextXAlignment.Left
CodeTitle.Parent = CodeFrame

local CodeTextBox = Instance.new("TextBox")
CodeTextBox.Size = UDim2.new(1, -20, 1, -55)
CodeTextBox.Position = UDim2.new(0, 10, 0, 45)
CodeTextBox.BackgroundColor3 = Color3.fromRGB(10, 11, 13)
CodeTextBox.BorderSizePixel = 0
CodeTextBox.Text = "-- Hãy thực hiện hành động trong game để bắt gói tin.\n-- Chọn Remote để lấy mã lập trình tương ứng."
CodeTextBox.TextColor3 = Color3.fromRGB(150, 215, 150)
CodeTextBox.Font = Enum.Font.Code
CodeTextBox.TextSize = 12
CodeTextBox.TextXAlignment = Enum.TextXAlignment.Left
CodeTextBox.TextYAlignment = Enum.TextYAlignment.Top
CodeTextBox.ClearTextOnFocus = false
CodeTextBox.TextEditable = false
CodeTextBox.MultiLine = true
CodeTextBox.Parent = CodeFrame

local ScrollingFrame = Instance.new("ScrollingFrame")
ScrollingFrame.Size = UDim2.new(1, -10, 1, -45)
ScrollingFrame.Position = UDim2.new(0, 5, 0, 40)
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollingFrame.ScrollBarThickness = 6
ScrollingFrame.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 4)
UIListLayout.Parent = ScrollingFrame

UIListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y)
end)

local capturedRemotes = {}
local activeUIButtons = {}

-- Bộ phân tích và dịch kiểu dữ liệu sang cấu trúc chuỗi code hợp lệ
local function serializeValue(val, depth)
    depth = depth or 1
    local indent = string.rep("    ", depth)
    local t = typeof(val)
    
    if t == "string" then 
        return string.format('"%s"', val)
    elseif t == "number" or t == "boolean" then 
        return tostring(val)
    elseif t == "Vector3" then 
        return string.format("Vector3.new(%f, %f, %f)", val.X, val.Y, val.Z)
    elseif t == "Vector2" then 
        return string.format("Vector2.new(%f, %f)", val.X, val.Y)
    elseif t == "CFrame" then 
        return string.format("CFrame.new(%s)", tostring(val))
    elseif t == "Color3" then 
        return string.format("Color3.fromRGB(%d, %d, %d)", val.R*255, val.G*255, val.B*255)
    elseif t == "Instance" then 
        return "game." .. val:GetFullName()
    elseif t == "table" then
        local str = "{\n"
        for k, v in pairs(val) do
            local keyPart = typeof(k) == "number" and "" or string.format("[%s] = ", serializeValue(k, depth + 1))
            str = str .. indent .. keyPart .. serializeValue(v, depth + 1) .. ",\n"
        end
        return str .. string.rep("    ", depth - 1) .. "}"
    else 
        return "nil --[[" .. t .. "]]" 
    end
end

-- Hàm dựng khối code lập trình dựa trên dữ liệu thu thập
local function renderCodeStructure(remote, logData)
    local path = remote:GetFullName()
    local cleanPath = path:sub(1,5) == "game." and path or "game." .. path
    
    local sb = "-- ĐOẠN MÃ LẬP TRÌNH ĐƯỢC KHỞI TẠO TỰ ĐỘNG\n"
    sb = sb .. string.format("local TargetRemote = %s\n\n", cleanPath)
    sb = sb .. "local Args = {\n"
    
    if logData.args and #logData.args > 0 then
        for i, arg in ipairs(logData.args) do
            sb = sb .. string.format("    [%d] = %s,\n", i, serializeValue(arg, 2))
        end
    else
        sb = sb .. "    -- Không chứa tham số truyền tải\n"
    end
    sb = sb .. "}\n\n"
    
    if logData.method == "FireServer" then
        sb = sb .. "TargetRemote:FireServer(unpack(Args))\n"
    else
        sb = sb .. "local success, result = pcall(function()\n"
        sb = sb .. "    return TargetRemote:InvokeServer(unpack(Args))\n"
        sb = sb .. "end)\n\n"
        sb = sb .. "if success then\n"
        sb = sb .. "    print(\"Gọi hàm thành công, phản hồi từ server:\", result)\n"
        sb = sb .. "else\n"
        sb = sb .. "    warn(\"Lỗi thực thi lệnh:\", result)\n"
        sb = sb .. "end\n"
    end
    return sb
end

local function registerOrUpdateLog(remote, method, args)
    if not remote or not remote.Name then return end
    
    capturedRemotes[remote] = {
        method = method,
        args = args
    }
    
    if activeUIButtons[remote] then return end

    local LogItem = Instance.new("TextButton")
    LogItem.Size = UDim2.new(1, -10, 0, 30)
    LogItem.BackgroundColor3 = method == "FireServer" and Color3.fromRGB(32, 44, 36) or Color3.fromRGB(46, 32, 34)
    LogItem.BorderSizePixel = 0
    LogItem.Text = string.format(" [%s] %s", method:sub(1,4):upper(), tostring(remote.Name))
    LogItem.TextColor3 = Color3.fromRGB(245, 245, 245)
    LogItem.Font = Enum.Font.Arial
    LogItem.TextSize = 12
    LogItem.TextXAlignment = Enum.TextXAlignment.Left
    LogItem.Parent = ScrollingFrame
    
    activeUIButtons[remote] = LogItem

    LogItem.MouseButton1Click:Connect(function()
        local currentData = capturedRemotes[remote]
        if currentData then
            CodeTextBox.Text = renderCodeStructure(remote, currentData)
        end
    end)
end

-- Bộ lọc Metamethod __namecall để tự động cập nhật code chính xác khi người chơi bấm nút/thao tác
local oldNamecall
pcall(function()
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        local method = getnamecallmethod()
        if method == "FireServer" or method == "InvokeServer" then
            local args = {...}
            task.defer(registerOrUpdateLog, self, method, args)
        end
        return oldNamecall(self, ...)
    end)
end)

print("[SimpleSpy-V13] Cấu hình trích xuất mã lập trình thời gian thực đã hoạt động.")
